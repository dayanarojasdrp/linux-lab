

# Práctica: Estructura del Sistema de ArchEntender la jerarquía del sistema deivos 

## Objetivo
 archivos Linux y principal.

## Concept el propósito de cada directorioos clave
- /
- /home
- /etc
- /var
- /usr
- /opt
- /tmp
- /root

## Ejercicios

### 1. Explorar directorios clave
```bash
ls -l /
ls -l /etc
ls -l /var
ls -l /usr
